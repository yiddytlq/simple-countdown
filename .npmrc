# NPM Performance Optimizations
# Prefer offline cache for faster installs
prefer-offline=true

# Disable progress bars for CI/CD environments
progress=false

# Disable audit during install for CI performance (run separately)
audit=false

# Use exact versions for reproducible builds
save-exact=true